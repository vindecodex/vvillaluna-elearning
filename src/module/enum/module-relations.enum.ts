/*
 * Used for validating on join dto that should contains one of these
 * Also used on mapping TableRelation on joinBuilder
 */
export enum ModuleRelations {
  AUTHOR = 'author',
  COURSE = 'course',
  CONTENT = 'content',
}
